<%= form_for([@competition, @average], :html => { :class => "form-stacked"}) do |f| %>
  <% if @average.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@average.errors.count, "error") %> prohibited this average from being saved:</h2>

      <ul>
      <% @average.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
