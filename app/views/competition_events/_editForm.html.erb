<%= form_for @competition_event, :url => competition_competition_event_path(@competition_event) do |f| %>
<% if @competition_event.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@competition_event.errors.count, "error") %> prohibited this event from being saved:</h2>

    <ul>
    <% @competition_event.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.label :competition_id %><br>
  <%= f.number_field :competition_id %>
</div>
<div class="field">
  <%= f.label :num_competitors %><br>
  <%= f.number_field :num_competitors %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
