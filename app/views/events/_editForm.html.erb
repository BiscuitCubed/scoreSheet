<%= form_for @event, :url => competition_event_path(@event) do |f| %>
<% if @event.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

    <ul>
    <% @event.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :name %><br>
  <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.label :competition_id %><br>
  <%= f.number_field :competition_id %>
</div>
<div class="field">
  <%= f.label :num_competitors %><br>
  <%= f.number_field :num_competitors %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
